// Configuration for the packaged app
// This ensures the API key is available if you intend to packaged the app
const config = {
  // Claude API Key for limited demo usage
  CLAUDE_API_KEY: '',
};

module.exports = config;
